Before using the scripts, update config.ini file with your infrastructure details. 

load_file metrics (FORMAT - load_file filename)
	
	It is the file calling all functions. 
	For each specific file in a bucket, one can create this file. 
	Functions can be reuse wherever needed.  
	

Next Step - we can go for looping if more than one file in a similar manner